import './index.scss'

export default function About() {
    return (
        <>
            <div>
                This is the about page that was accidentally deleted so that matty can do work on it (i hpe) so hang in there its coming
            </div>
        </>
    )
}

